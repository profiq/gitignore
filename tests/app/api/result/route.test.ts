// import { GET } from "../../../../app/api/result/route";
// import { NextRequest } from "next/server";

// let mockImplementation: Promise<string> = Promise.resolve("mocked result");

// jest.mock("../../../../app/api/lib/result", () => ({
//   getResult: jest.fn(() => mockImplementation)
// }))

// describe("GET /api/result", () => {
//   let req: NextRequest

//   beforeEach(() => {
//     req = new NextRequest("/api/result")
//   })

//   test("should return 200", async () => {
//     const response = await GET(req);
//     debugger
//     console.log(response)

//     //expect(response.status).toBe(200);
//   });
// })
